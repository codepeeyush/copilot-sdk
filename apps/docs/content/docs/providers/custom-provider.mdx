---
title: Custom Provider
description: Build your own LLM provider adapter
---

import { Callout } from 'fumadocs-ui/components/callout';

<Callout type="warn">
**Coming Soon.** Custom provider documentation is under active development.
</Callout>

# Custom Provider

Build an adapter for any LLM API that isn't natively supported.

Custom providers follow the same interface as built-in providers. Your frontend code doesn't need to change when using a custom provider.
