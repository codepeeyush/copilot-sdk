---
title: React
description: React hooks, provider, and components
---

import { Cards, Card } from 'fumadocs-ui/components/card';

# React Package

Full React integration with hooks and components.

```bash
npm install @yourgpt/copilot-sdk/react
```

---

<Cards>
  <Card title="Hooks" href="/docs/api-reference/react/hooks">
    useCopilot, useTools, useAIContext
  </Card>
  <Card title="Components" href="/docs/api-reference/react/components">
    CopilotProvider and context components
  </Card>
</Cards>

---

## Quick Start

```tsx
import { CopilotProvider, useCopilot, useTools } from '@yourgpt/copilot-sdk/react';
import { CopilotChat } from '@yourgpt/copilot-sdk/react';

function App() {
  return (
    <CopilotProvider runtimeUrl="/api/chat">
      <MyTools />
      <CopilotChat />
    </CopilotProvider>
  );
}

function MyTools() {
  useTools({
    search: {
      description: 'Search products',
      parameters: z.object({ query: z.string() }),
      handler: async ({ query }) => searchProducts(query),
    },
  });
  return null;
}
```
